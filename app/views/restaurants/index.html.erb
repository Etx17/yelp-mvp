<h1>Restaurants</h1>

<ul>
<% @restaurants.each do |restaurant| %>
  <li>
    <%= restaurant.name %>
    - <%= restaurant.address %>
    - <%= restaurant.category %>
    - <%= link_to "Show details", restaurant_path(restaurant) %>
  </li>
<% end %>
</ul>
